/*
#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <math.h>

void main()
{
	double a,b,c,e, f, g, h;
	scanf("%lf%lf%lf%lf", &e, &f, &g, &h);
	a = (e + f / 2) / 3;
	b = abs(pow(h, 2) - g);
	c = sqrt(pow(g - h, 2) - 3 * sin(e));
	printf("%lf\n%lf\n%lf", a, b, c);
}
*/